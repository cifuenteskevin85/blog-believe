defaults: &defaults
  adapter: postgresql
  template: template0

development:
  <<: *defaults
  database: blog-believe_development

test:
  <<: *defaults
  min_messages: warning #magic sauce
  database: blog-believe_test
